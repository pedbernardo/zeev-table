import { createTable } from './instance'

import {
  reset,
  appendData,
  enable,
  disable
} from './utils/actions'

import {
  getRows,
  getLastRow,
  getLength,
  getData,
  getRowData
} from './utils/dom'

export {
  // instance
  createTable,

  // actions
  reset,
  appendData,
  enable,
  disable,

  // dom
  getRows,
  getLastRow,
  getLength,
  getData,
  getRowData
}
